"use strict";

function playExplosion(n, x, y) {
    switch (n) {
    case 0:
        healthDrop(x, y);
        break;
    case 1:
        healthDrop(x, y);
        break;
    case 2:
        break;
    }
}